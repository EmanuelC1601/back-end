{
  "name": "backend-proyecto-angular",
  "version": "1.0.0",
  "description": "Backend para proyecto Angular con MySQL",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:init": "mysql -u root -p < sql/init.sql || echo 'Asegúrate de tener MySQL instalado y configurado'",
    "db:reset": "mysql -u root -p -e 'DROP DATABASE IF EXISTS proyecto_angular; CREATE DATABASE proyecto_angular; USE proyecto_angular; source sql/init.sql;' || echo 'Error al resetear BD'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "angular",
    "nodejs",
    "express",
    "mysql",
    "backend",
    "api",
    "rest"
  ],
  "author": "Tu Nombre",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^3.6.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  }
}